rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to facility tracker snapshots
    match /facility_trackers/{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
