rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to facility trackers collection
    match /facility_trackers/{document=**} {
      allow read, write: if true;
    }
  }
}
